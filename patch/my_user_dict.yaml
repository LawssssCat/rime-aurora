patch:
  # 保持为第一个 translator，这样用户词典的词就会显示在前面
  engine/translators/@before 0: lua_translator@my_user_dict_translator 
  my_user_dict_translator:
    # 指定的 tag 不进行音节分词
    excluded_syllabify_tags: ["url", "symbals"]