patch:
  engine/filters/+: 
    # - uniquifier
    - lua_filter@my_uniquifier_filter # 去重。替代uniquifier
  my_uniquifier_filter:
    excluded_types: ["history"]
    # 在前多少个候选词中去重
    uniquifier_max: 100 # 200 不卡 300 有点卡 1000 能用